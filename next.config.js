/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'encurtador.com.br',
          'media.graphassets.com',
          'github.com'
        ]
    }
}

module.exports = nextConfig
